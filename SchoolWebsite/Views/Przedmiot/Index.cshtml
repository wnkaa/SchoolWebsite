@model IEnumerable<SchoolWebsite.Models.Przedmiot>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_LayoutPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Nazwa gatunku
        </th>
        <th>
            Nazwa przedmiotu
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Specie.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrzedmiotID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrzedmiotID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrzedmiotID })
        </td>
    </tr>
}

</table>
